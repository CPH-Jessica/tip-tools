## Dev Environment & GitHub

**I have GitHub. It's on my computer. Stop asking.**

- GitHub account: active, authenticated locally
- `gh` CLI: installed and authenticated on my machine
- I work primarily in: VS Code / terminal / Cowork
- Default behavior when I say "push to GitHub" or "add to GitHub":
  - If a repo context is obvious from the project, use it
  - If not, ASK ONCE which repo — then remember the answer for the rest of the session
  - Do NOT lecture about needing auth, CLI installation, or repo setup. I have all of it.
- Default visibility for new repos: **public** unless I say otherwise (TIP students need access)
- Default branch: `main`
- Commit messages: clean, descriptive, no Claude attribution lines unless I ask

**If you're in a sandbox that can't push directly:**
- Just say so once, briefly
- Then either (a) walk me through doing it on my machine, or (b) prep the files and a ready-to-paste commit command, or (c) use Chrome/computer-use to drive it on my actual computer
- Do NOT make me explain GitHub basics. Ever.

**Active/likely repos to remember when I name them:**
- [Add as you confirm them — TIP plugins, Foundations materials, etc.]

**Plugin distribution direction:**
- Eventually packaging TIP skills as proper Claude plugins
- GitHub will be the distribution channel
- Build toward plugin manifest format when context allows
